/* Crea un arreglo en JavaScript con nombres de países. Utiliza 
un bucle para recorrer el arreglo y mostrar los  nombres en la consola. */

var paises = ["Estados Unidos", "Canadá", "México", "Argentina", "España", "Francia"];

for (var i = 0; i < paises.length; i++) {
    console.log(paises[i]);
}